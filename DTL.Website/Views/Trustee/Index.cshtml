@using DTL.Model.Models.CMS

@{
    ViewData["Title"] = "Board Of Trutee";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@model IEnumerable<TrusteeModel>;
<section id="header_section">
    <div class="container">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <h1>Board of Trustee</h1>
                <p><a href="@Url.Action("","home")">Home</a> / <span>About Us / Board of Trustee</span></p>

            </div>
        </div>
    </div>
</section>

<section id="main_page">
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <div class="sidebar">

                    <div class="tab">
                        <a class="tablinks" href="@Url.Action("","about")">About</a>
                        <a class="tablinks active" href="#">
                            Board of Trustee
                        </a>
                    </div>
                </div>
            </div>
            <div class="col-md-12">
                <div class="main_content">
                    <div class="tabcontent hospital_list">
                        <div class="team-section">
                            <div class="inner-width">
                                @if (Model.Count() > 0)
                                {
                                    int counter = 0;
                                    @foreach (var item in Model)
                                    {
                                        counter++;
                                        <div class="pers" id="@item.ID">


                                            <div class="pe ">
                                                @{
                                                    string Image = "/assets/images/bg.jpg";
                                                }
                                                @if (item.Image != null)
                                                {
                                                    var base64 = Convert.ToBase64String(item.Image);
                                                    Image = String.Format("data:{0};base64,{1}", item.ImageContentType, base64);
                                                }
                                                @*src="https://us.123rf.com/450wm/rmarmion/rmarmion1402/rmarmion140200019/26326734-businesswoman-working-at-her-office-by-herself.jpg?ver=6"*@
                                                <img src="@Image" alt="rafeh" class="shadow">
                                                <div class="description">
                                                    <div class="p-name">@item.NameEnglish</div>
                                                    <div class="p-des">@item.PositionEnglish</div>

                                                    <div class="p-number">@item.Phone</div>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                }
                            </div>
                        </div>
                        @*<table class="table table-bordered table-hover" id="myTable">
                        <thead>
                        <tr>
                        <th>Sr. No.</th>
                        <th>Name</th>
                        <th>Position</th>
                        <th>Phone Number</th>
                        <th>Image</th>
                        </tr>
                        </thead>
                        <tbody>
                        @if (Model.Count() > 0)
                        {
                        int counter = 0;
                        @foreach (var item in Model)
                        {
                        counter++;
                        <tr id="@item.ID">
                        <td>@counter</td>
                        <td>
                        @item.NameEnglish
                        </td>
                        <td>
                        @item.PositionEnglish
                        </td>
                        <td>
                        @item.Phone
                        </td>
                        <td>
                        @{
                        string Image = "/assets/images/bg.jpg";
                        }
                        @if (item.Image != null)
                        {
                        var base64 = Convert.ToBase64String(item.Image);
                        Image = String.Format("data:{0};base64,{1}", item.ImageContentType, base64);
                        }
                        <img src="@Image" class="about_image shadow">
                        </td>

                        </tr>
                        }
                        }

                        </tbody>
                        </table>*@
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


